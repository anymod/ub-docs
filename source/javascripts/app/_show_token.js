/**
 * Show the Project Token for a user
 */
$(function() {
  $("#show-token").on("click", function() {
    alert(
      "This is where we check the cookie and make an API call to get the Project Token, then replace it everywhere on the page."
    );
  });
});
