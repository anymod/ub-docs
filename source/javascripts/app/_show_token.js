/**
 * Show the API key for a project
 */
$(function () {
  $("#show-token").on("click", function () {
    alert(
      "This is where we check the cookie and make an API call to get the API key, then replace it everywhere on the page."
    );
  });
});
